# USE codeup_test_db;
#
# SELECT * FROM albums;
#
# UPDATE albums SET sales = sales * 10;
#
# SELECT * FROM albums WHERE released_date < 1980;
#
# UPDATE albums SET released_date = released_date -100
# WHERE released_date < 1980;
#
# SELECT * FROM albums WHERE artist = 'Michael Jackson';
#
# UPDATE albums SET artist = 'Peter Jackson'
# WHERE artist = 'Michael Jackson';